<%= form_for(@noticia) do |f| %>
  <% if @noticia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@noticia.errors.count, "error") %> prohibited this noticia from being saved:</h2>

      <ul>
      <% @noticia.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :avance %><br />
    <center><%= f.text_area :avance, :class => "mceEditor","cols" => 50, "rows" => 20 %></center>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <center><%= f.text_area :body, :class => "mceEditor","cols" => 100, "rows" => 40 %></center>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
