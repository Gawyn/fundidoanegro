<div id = "contenedorcriticasindex">
<div id="demo"> 
		<% i=0 %>
		<table align="center">
		<tr>
		<% @especials.each do |e| %>
			<td><%= link_to image_tag(e.image.url, :title => e.title+' por '+e.autor, :size => "350x150"), e %></td>		
  				<% if i==2 %>
				</tr>
				<tr>
				<% end %>
			<% i=i+1 %>
		<% end %>
		</tr>
		</table>
</div>
</div>

<script> 
$("#demo img[title]").tooltip();
</script> 
